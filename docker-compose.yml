services:
  frontend:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env.development      # используем удалённые настройки
    environment:
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true